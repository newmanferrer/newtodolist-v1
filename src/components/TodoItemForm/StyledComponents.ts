//* ///////////////////////////////////////////////////////////////////////////
//* TodoItemForm
//* ///////////////////////////////////////////////////////////////////////////
//* ===========================================================================
//* 1.- Imports
//* ===========================================================================
import styled from 'styled-components';
import { COLORS } from '../../colors';
import { GrFlagFill } from 'react-icons/gr';
import { GoDiffAdded } from 'react-icons/go';
import { FaEdit, FaRegCircle, FaRegCheckCircle } from 'react-icons/fa';
import { GiCancel } from 'react-icons/gi';
import { RiDeleteBinFill } from 'react-icons/ri';
//* ===========================================================================

//* ===========================================================================
//* 2.- Models / Interfaces
//* ===========================================================================
//* ===========================================================================

//* ===========================================================================
//* 3.- Reusable Components
//* ===========================================================================
//* ---------------------------------------------------------------------------
//* 3.1.- Container
//* ---------------------------------------------------------------------------
const Container = styled.div`
 display: flex;
 flex-direction: column;
 flex-wrap: nowrap;
 justify-content: center;
 align-items: center;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 3.2.- Title
//* ---------------------------------------------------------------------------
const Title = styled.h2`
 font-size: 1.2rem;
 font-weight: bold;
 text-align: center;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 3.3.- Label
//* ---------------------------------------------------------------------------
const Label = styled.label``;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 3.4.- Input
//* ---------------------------------------------------------------------------
const Input = styled.input`
 width: 100%;
 padding: 0.25rem;

 font-family: sans-serif;
 font-size: 1rem;
 outline: none;

 border: none;
 border-radius: 0.25rem;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 3.5.- Button
//* ---------------------------------------------------------------------------
const Button = styled.button`
 padding: 0.35rem 1rem;

 display: flex;
 flex-direction: column;
 flex-wrap: nowrap;
 justify-content: center;
 align-items: center;

 font-size: 1rem;
 outline: none;
 transition: all 0.5s ease-in-out;

 border: 1px solid transparent;
 border-radius: 0.25rem;

 &:hover {
  cursor: pointer;
  color: ${COLORS.hoverColor};
  border: 1px solid ${COLORS.hoverColor};
  background-color: ${COLORS.backgroundDark};
 }
`;
//* ---------------------------------------------------------------------------
//* ===========================================================================

//* ===========================================================================
//* 4.- Used components
//* ===========================================================================
//* ---------------------------------------------------------------------------
//* 4.1.- Main Container
//* ---------------------------------------------------------------------------
const MainContainer = styled(Container)`
 width: 100%;
 padding: 0.25rem;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.2.- Form
//* ---------------------------------------------------------------------------
const Form = styled.form`
 width: 100%;
 padding: 1.5rem 0.5rem;

 display: flex;
 flex-direction: column;
 flex-wrap: nowrap;
 justify-content: center;
 align-items: center;

 border-radius: 5px;
 background-color: ${COLORS.backgroundDark};
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.3.- Title Wrapper
//* ---------------------------------------------------------------------------
const TitleWrapper = styled(Container)`
 width: 100%;
 margin-bottom: 1rem;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.4.- Main Title
//* ---------------------------------------------------------------------------
const MainTitle = styled(Title)`
 font-size: 1.2rem;
 font-weight: bold;
 color: ${COLORS.hoverColor};
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.5.- Inputs Wrapper
//* ---------------------------------------------------------------------------
const InputsWrapper = styled(Container)`
 width: 100%;
 padding: 0.5rem;
 margin-bottom: 1rem;

 border-radius: 3px;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.6.- Input Title
//* ---------------------------------------------------------------------------
const InputTitle = styled(Input)<any>`
 margin-bottom: 0.5rem;
 font-size: 0.9rem;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.7.- Text Area
//* ---------------------------------------------------------------------------
const TextArea = styled.textarea.attrs({
 cols: 30,
 rows: 2
})`
 width: 100%;
 padding: 0.25rem;
 margin-bottom: 0.5rem;

 font-family: sans-serif;
 font-size: 0.9rem;
 outline: none;
 resize: none;

 border: none;
 border-radius: 0.25rem;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.8.- Priority Wrapper
//* ---------------------------------------------------------------------------
const PriorityWrapper = styled(Container)`
 width: 100%;
 padding: 0.25rem;
 margin-bottom: 0.5rem;

 flex-direction: row;
 justify-content: space-between;

 border-radius: 3px;
 border: 1px solid ${COLORS.hoverColor};
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.9.- PriorityLabel
//* ---------------------------------------------------------------------------
const PriorityLabel = styled(Label)`
 font-size: 0.9rem;
 font-weight: bold;
 color: ${COLORS.hoverColor};
 cursor: pointer;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.10.- InputPriority
//* ---------------------------------------------------------------------------
const InputPriority = styled(Input)`
 appearance: none;
 display: none;

 &:checked + label {
  padding: 0.35rem;
  background-color: ${COLORS.hoverColor};
  border: 1px solid ${COLORS.white};
  border-radius: 3px;
 }
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.11.- IconPrioritylabel
//* ---------------------------------------------------------------------------
const IconPrioritylabel = styled(Label)`
 padding: 0.35rem;
 border: 1px solid transparent;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.12.- PriorityNormalIcon
//* ---------------------------------------------------------------------------
const PriorityNormalIcon = styled(GrFlagFill)`
 display: flex;
 flex-direction: column;
 flex-wrap: nowrap;
 justify-content: center;
 align-items: center;

 color: ${COLORS.success};
 transition: all 0.7s ease-in-out;

 &:hover {
  cursor: pointer;
  transform: scale(1.1);
 }
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.13.- PriorityMediumIcon
//* ---------------------------------------------------------------------------
const PriorityMediumIcon = styled(GrFlagFill)`
 display: flex;
 flex-direction: column;
 flex-wrap: nowrap;
 justify-content: center;
 align-items: center;

 color: ${COLORS.warning};
 transition: all 0.7s ease-in-out;

 &:hover {
  cursor: pointer;
  transform: scale(1.2);
 }
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.14.- PriorityHighIcon
//* ---------------------------------------------------------------------------
const PriorityHighIcon = styled(GrFlagFill)`
 display: flex;
 flex-direction: column;
 flex-wrap: nowrap;
 justify-content: center;
 align-items: center;

 color: ${COLORS.error};
 transition: all 0.7s ease-in-out;

 &:hover {
  cursor: pointer;
  transform: scale(1.2);
 }
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.15.- StartDateWrapper
//* ---------------------------------------------------------------------------
const StartDateWrapper = styled(Container)`
 width: 100%;
 padding: 0.25rem;
 margin-bottom: 0.5rem;

 color: ${COLORS.hoverColor};
 border-radius: 3px;
 border: 1px solid ${COLORS.hoverColor};
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.16.- StartDateLabelWrapper
//* ---------------------------------------------------------------------------
const StartDateLabelWrapper = styled(Container)`
 width: 100%;
 flex-direction: row;
 justify-content: flex-start;
 font-size: 0.9rem;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.17.- StartDateLabel
//* ---------------------------------------------------------------------------
const StartDateLabel = styled(Label)`
 margin-right: 1rem;
 margin-bottom: 0.25rem;
 font-size: 0.9rem;
 font-weight: bold;
 color: ${COLORS.hoverColor};
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.18.- DueDateWrapper
//* ---------------------------------------------------------------------------
const DueDateWrapper = styled(Container)`
 width: 100%;
 padding: 0.25rem;
 margin-bottom: 0.5rem;
 color: ${COLORS.hoverColor};

 border-radius: 3px;
 border: 1px solid ${COLORS.hoverColor};
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.19.- DueDateLabelWrapper
//* ---------------------------------------------------------------------------
const DueDateLabelWrapper = styled(Container)`
 width: 100%;
 align-items: flex-start;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.20.- DueDateLabel
//* ---------------------------------------------------------------------------
const DueDateLabel = styled(Label)`
 margin-bottom: 0.25rem;
 font-size: 0.9rem;
 font-weight: bold;
 cursor: pointer;
`;
//* ---------------------------------------------------------------------------
//* ---------------------------------------------------------------------------
//* 4.21.- InputDueDate
//* ---------------------------------------------------------------------------
const InputDueDate = styled(Input)``;
//* ---------------------------------------------------------------------------
//* ---------------------------------------------------------------------------
//* 4.22.- CompletedWrapper
//* ---------------------------------------------------------------------------
const CompletedWrapper = styled(Container)`
 width: 100%;
 padding: 0.5rem 0.25rem;
 margin-bottom: 0.5rem;

 flex-direction: row;
 justify-content: flex-start;

 border-radius: 3px;
 border: 1px solid ${COLORS.hoverColor};
`;
//* ---------------------------------------------------------------------------
//* ---------------------------------------------------------------------------
//* 4.23.- LabelCompleted
//* ---------------------------------------------------------------------------
const LabelCompleted = styled(Label)`
 font-size: 0.9rem;
 font-weight: bold;
 color: ${COLORS.hoverColor};
 cursor: pointer;
`;
//* ---------------------------------------------------------------------------
//* ---------------------------------------------------------------------------
//* 4.24.- InputCompleted
//* ---------------------------------------------------------------------------
const InputCompleted = styled(Input)`
 display: none;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.25.- IconCompletedlabel
//* ---------------------------------------------------------------------------
const IconCompletedlabel = styled(Label)`
 margin-left: 1rem;

 display: flex;
 flex-direction: column;
 flex-wrap: nowrap;
 justify-content: center;
 align-items: center;

 cursor: pointer;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.26.- FaRegCircleIcon
//* ---------------------------------------------------------------------------
const FaRegCircleIcon = styled(FaRegCircle)`
 font-size: 1.3rem;
 color: ${COLORS.error};
 transition: color 0.5s ease-in-out;

 &:hover {
  color: ${COLORS.success};
 }
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.27.- FaRegCheckCircleIcon
//* ---------------------------------------------------------------------------
const FaRegCheckCircleIcon = styled(FaRegCheckCircle)`
 font-size: 1.3rem;
 color: ${COLORS.success};
 transition: color 0.5s ease-in-out;

 &:hover {
  color: ${COLORS.error};
 }
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.28.- ButtonsWrapper
//* ---------------------------------------------------------------------------
const ButtonsWrapper = styled(Container)`
 width: 100%;

 flex-direction: row;
 justify-content: space-evenly;

 border-radius: 3px;
`;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.29.- AddIcon
//* ---------------------------------------------------------------------------
const AddIcon = styled(GoDiffAdded)``;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.30.- EditIcon
//* ---------------------------------------------------------------------------
const EditIcon = styled(FaEdit)``;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.31.- DeleteIcon
//* ---------------------------------------------------------------------------
const DeleteIcon = styled(RiDeleteBinFill)``;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.32.- CancelIcon
//* ---------------------------------------------------------------------------
const CancelIcon = styled(GiCancel)``;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.33.- ButtonAddTodo
//* ---------------------------------------------------------------------------
const ButtonAddTodo = styled(Button).attrs({
 title: 'Add todo'
})``;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.34.- ButtonEditTodo
//* ---------------------------------------------------------------------------
const ButtonEditTodo = styled(Button).attrs({
 title: 'Edit todo'
})``;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.35.- ButtonDeleteTodo
//* ---------------------------------------------------------------------------
const ButtonDeleteTodo = styled(Button).attrs({
 title: 'Delete todo'
})``;
//* ---------------------------------------------------------------------------

//* ---------------------------------------------------------------------------
//* 4.36.- ButtonCancel
//* ---------------------------------------------------------------------------
const ButtonCancel = styled(Button).attrs({
 title: 'Cancel'
})``;
//* ---------------------------------------------------------------------------
//* ===========================================================================

//* ===========================================================================
//* 5.- Exported components
//* ===========================================================================
export {
 MainContainer,
 Form,
 TitleWrapper,
 MainTitle,
 InputsWrapper,
 Label,
 InputTitle,
 TextArea,
 PriorityWrapper,
 PriorityLabel,
 IconPrioritylabel,
 InputPriority,
 PriorityNormalIcon,
 PriorityMediumIcon,
 PriorityHighIcon,
 StartDateWrapper,
 StartDateLabelWrapper,
 StartDateLabel,
 DueDateWrapper,
 DueDateLabelWrapper,
 DueDateLabel,
 InputDueDate,
 CompletedWrapper,
 LabelCompleted,
 InputCompleted,
 IconCompletedlabel,
 FaRegCircleIcon,
 FaRegCheckCircleIcon,
 ButtonsWrapper,
 AddIcon,
 EditIcon,
 DeleteIcon,
 CancelIcon,
 ButtonAddTodo,
 ButtonEditTodo,
 ButtonDeleteTodo,
 ButtonCancel
};
//* ===========================================================================
//* ///////////////////////////////////////////////////////////////////////////
